/**
 * Wrapper classes for the executions asset grid (deliverables thumbnail modes).
 * Intentionally empty: avoid tinted panel + ring so the grid matches the rest of the page.
 *
 * @param {'standard' | 'original' | 'enhanced' | 'presentation' | 'clean' | null | undefined} executionThumbnailViewMode
 */
export function executionEnhancedGridContainerClass(_executionThumbnailViewMode) {
    return ''
}
